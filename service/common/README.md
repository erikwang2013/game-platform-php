# common/

`common\service\*` lives in the shared package **erik/platform-common**:

`packages/platform-common`

Do not put PHP classes in this directory (they would shadow the package autoload). See `packages/platform-common/README.md`.
